apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: circleguard-gateway-tls
  namespace: circleguard-dev
spec:
  secretName: circleguard-gateway-tls-secret
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
  dnsNames:
    - circleguard.local
    - localhost
