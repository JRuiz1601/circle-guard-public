apiVersion: v1
kind: Service
metadata:
  name: circleguard-dashboard-service
  namespace: circleguard-dev
  labels:
    app: circleguard-dashboard-service
spec:
  type: ClusterIP
  selector:
    app: circleguard-dashboard-service
  ports:
    - name: http
      port: 80
      targetPort: 8085
