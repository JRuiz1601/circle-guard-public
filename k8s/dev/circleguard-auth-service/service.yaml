apiVersion: v1
kind: Service
metadata:
  name: circleguard-auth-service
  namespace: circleguard-dev
  labels:
    app: circleguard-auth-service
spec:
  type: ClusterIP
  selector:
    app: circleguard-auth-service
  ports:
    - name: http
      port: 80
      targetPort: 8081
