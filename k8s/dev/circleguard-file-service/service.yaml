apiVersion: v1
kind: Service
metadata:
  name: circleguard-file-service
  namespace: circleguard-dev
spec:
  type: ClusterIP
  selector:
    app: circleguard-file-service
  ports:
    - port: 80
      targetPort: 8084
