apiVersion: v1
kind: Service
metadata:
  name: circleguard-file-service
  namespace: circleguard-dev
  labels:
    app: circleguard-file-service
spec:
  type: ClusterIP
  selector:
    app: circleguard-file-service
  ports:
    - name: http
      port: 80
      targetPort: 8084
