apiVersion: v1
kind: Service
metadata:
  name: circleguard-identity-service
  namespace: circleguard-dev
  labels:
    app: circleguard-identity-service
spec:
  type: ClusterIP
  selector:
    app: circleguard-identity-service
  ports:
    - name: http
      port: 80
      targetPort: 8082
