apiVersion: v1
kind: Service
metadata:
  name: circleguard-form-service
  namespace: circleguard-dev
spec:
  type: ClusterIP
  selector:
    app: circleguard-form-service
  ports:
    - port: 80
      targetPort: 8083
