apiVersion: v1
kind: Service
metadata:
  name: circleguard-promotion-service
  namespace: circleguard-dev
spec:
  type: ClusterIP
  selector:
    app: circleguard-promotion-service
  ports:
    - port: 80
      targetPort: 8088
