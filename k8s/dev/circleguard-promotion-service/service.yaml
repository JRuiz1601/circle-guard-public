apiVersion: v1
kind: Service
metadata:
  name: circleguard-promotion-service
  namespace: circleguard-dev
  labels:
    app: circleguard-promotion-service
spec:
  type: ClusterIP
  selector:
    app: circleguard-promotion-service
  ports:
    - name: http
      port: 80
      targetPort: 8088
