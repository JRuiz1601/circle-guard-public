apiVersion: v1
kind: Service
metadata:
  name: circleguard-file-service
  namespace: circleguard-stage
spec:
  type: ClusterIP
  selector:
    app: circleguard-file-service
  ports:
    - port: 80
      targetPort: 8084
