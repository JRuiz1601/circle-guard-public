apiVersion: v1
kind: Service
metadata:
  name: circleguard-gateway-service
  namespace: circleguard-stage
spec:
  type: NodePort
  selector:
    app: circleguard-gateway-service
  ports:
    - port: 80
      targetPort: 8080
