apiVersion: v1
kind: Service
metadata:
  name: circleguard-dashboard-service
  namespace: circleguard-stage
spec:
  type: ClusterIP
  selector:
    app: circleguard-dashboard-service
  ports:
    - port: 80
      targetPort: 8085
