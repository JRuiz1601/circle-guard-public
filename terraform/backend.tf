# El backend remoto (HCP Terraform) se declara en cada environments/*/main.tf
# dentro del bloque `terraform { backend "remote" { ... } }`.
#
# Este archivo existe como referencia y punto de entrada para terraform fmt.
# No añadir aquí configuración funcional.
